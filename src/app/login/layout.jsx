export const metadata = {
  title: "Sign In | DriveFleet",
  description:
    "Log in to your DriveFleet account to manage your car bookings, updates, and profile.",
};

export default function SignInLayout({ children }) {
  return <>{children}</>;
}